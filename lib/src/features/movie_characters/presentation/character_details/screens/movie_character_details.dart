import 'package:flutter/material.dart';

class MovieCharacterDetails extends StatelessWidget {
  const MovieCharacterDetails({super.key});

  @override
  Widget build(BuildContext context) => const Placeholder();
}
